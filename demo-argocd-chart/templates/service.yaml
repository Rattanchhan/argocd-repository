apiVersion: v1
kind: Service
metadata:
  name: {{ include "demo-argocd-chart.fullname" . }}
  labels:
    {{- include "demo-argocd-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "demo-argocd-chart.selectorLabels" . | nindent 4 }}
