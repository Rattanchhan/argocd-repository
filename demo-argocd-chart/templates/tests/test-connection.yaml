apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "demo-argocd-chart.fullname" . }}-test-connection"
  labels:
    {{- include "demo-argocd-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "demo-argocd-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
